{
  "name": "Flutter Ultimate Workspace",
  "image": "ghcr.io/cirruslabs/flutter:stable",
  "postCreateCommand": "git config --global --add safe.directory '*' && flutter channel stable && flutter upgrade --force && flutter config --enable-web && flutter pub upgrade --major-versions",
  "customizations": {
    "vscode": {
      "extensions": [
        "dart-code.flutter",
        "dart-code.dart-code"
      ],
      "settings": {
        "editor.formatOnSave": true
      }
    }
  }
}
